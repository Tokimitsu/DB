1. Написать все наше пройденные ограничение, и написать своими словами кто за что отвечает, представьте вы это объясните своему другу: 

PRIMARY KEY (Первичный ключ):

За что отвечает: Ограничение PRIMARY KEY используется для уникальной идентификации каждой записи в таблице.
Как пишется: При создании таблицы, указывается столбец (или несколько столбцов), который будет использоваться в качестве первичного ключа. Например: PRIMARY KEY (id). Первичный ключ не может содержать дублирующиеся или NULL значения.

FOREIGN KEY (Внешний ключ):

За что отвечает: Ограничение FOREIGN KEY используется для связи двух таблиц на основе значения в столбце одной таблицы, ссылающемся на столбец в другой таблице.
Как пишется: При создании таблицы, указывается столбец, который будет использоваться в качестве внешнего ключа, и таблица, на которую он ссылается. Например: FOREIGN KEY (user_id) REFERENCES Users(id). Внешний ключ обеспечивает целостность данных и связь между таблицами.

UNIQUE (Уникальное значение):

За что отвечает: Ограничение UNIQUE используется для гарантии уникальности значений в указанном столбце или группе столбцов.
Как пишется: При создании таблицы, указывается столбец (или несколько столбцов), для которых нужно гарантировать уникальность значений. Например: UNIQUE (email). Уникальное ограничение предотвращает вставку дублирующихся значений в указанный столбец.

NOT NULL (Не может быть NULL):

За что отвечает: Ограничение NOT NULL используется для запрета значений NULL в указанном столбце.
Как пишется: При создании таблицы, указывается столбец, для которого требуется, чтобы значения не были NULL. Например: age INT NOT NULL. Это ограничение гарантирует, что в столбец будет вставлено непустое значение.


2. Напишите своими словами что такое дефолтное значение и приведите пример

Дефолтное значение (или значение по умолчанию) - это значение, которое будет автоматически присвоено полю или параметру, если явное значение не указано при создании записи или при обновлении значения поля.

Например, представим, что у нас есть таблица "Пользователи" с полем "Статус", которое указывает на текущий статус активности пользователя. Мы можем задать дефолтное значение этому полю, например, "Активен". Таким образом, если мы создаем нового пользователя и не указываем его статус, полю "Статус" будет автоматически присвоено значение "Активен".

CREATE TABLE Пользователи (
    id INT PRIMARY KEY,
    имя VARCHAR(50),
    статус VARCHAR(10) DEFAULT 'Активен'
);


3. Напишите своими словами что такое проверка в MYSQL, и как она пишется в Инициализация таблицы

CHECK (Проверка условия):

За что отвечает: Ограничение CHECK используется для определения условия, которое значения в столбце должны удовлетворять.
Как пишется: При создании таблицы, указывается столбец и условие, которому должны соответствовать значения. 
Пример :  
CREATE TABLE Сотрудники (
    id INT PRIMARY KEY,
    имя VARCHAR(50),
    возраст INT,
    пол VARCHAR(10),
    должность VARCHAR(50),
    зарплата DECIMAL(10,2),
    CHECK (возраст >= 18 AND зарплата > 0)
);
